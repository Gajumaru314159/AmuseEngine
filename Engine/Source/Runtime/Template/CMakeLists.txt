project(Template)

# 依存ライブラリ
set(public_libs)
set(private_libs)

# 依存ファイル
set(copy_files)

# インクルードディレクトリ
set(public_includes)
set(private_includes)

# 定義
set(public_definitions)
set(private_definitions)



# プロジェクトをライブラリとして追加する場合は以下の行をコメントアウトしてください
# amuse_add_library()

# プロジェクトを実行ファイルとして追加する場合は以下の行をコメントアウトしてください
# amuse_add_executable()

# プロジェクトをテストとして追加する場合は以下の行をコメントアウトしてください
# amuse_add_test()