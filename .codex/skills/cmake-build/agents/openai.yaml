interface:
  display_name: "CMake Build"
  short_description: "Build with direct CMake Ninja commands."
  default_prompt: "Use $cmake-build to configure, build, and test this repository with direct CMake Ninja commands."
